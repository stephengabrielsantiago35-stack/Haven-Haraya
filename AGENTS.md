# AGENTS.md — Working Summary

## Goal
Build and maintain a multi‑page brochure website for the "Haven Haraya" Airbnb condo listing in Taguig, Philippines.

## Constraints & Preferences
- Website must match the Airbnb listing's real pricing — minimum 2 nights at AED 698 (₱11,800), no per‑night or made‑up weekly rates
- Testimonials must use actual Airbnb review text
- All existing features (scroll meter, lightbox, gallery tabs, count‑up stats, ambient audio, scroll reveal) must keep working
- Guidebook content comes from the host's Airbnb guidebooks; FAQ is on its own page
- Background images must be context‑relevant with proper focal‑point positioning
- Scroll behavior targets danubeproperties.com: CSS fixed‑background parallax on `.bg-cover` sections + JS‑driven scroll transforms on content containers

## Progress
### Done
- **Mobile & tablet responsive overhaul** (all 8 pages):
  - **Tablet breakpoint (≤1024px)**: nav-links tighter; h1 5.2rem, h2 3.2rem; 4-col grids (page-card, detail) → 2-col; 3-col grids (feature, testimonial) → 2-col; pricing 3-col → 2-col with featured spanning full width; gallery layout tightened; timeline 2.4rem
  - **Mobile breakpoint (≤860px)**: hamburger menu; horizontal scroll nav pills; hero becomes flex-column 100svh; hero-content 1 col; hero-stats inline at bottom; 2-col grids → 1-col; 44px touch targets for menu-toggle, audio, tabs, form inputs; 16px form input font-size (prevents iOS zoom); lightbox 48px nav buttons; FAQ question 1rem; 44px tab buttons
  - **Small mobile (≤620px)**: h1 2.8rem, h2 2.1rem; gallery 2-col 180px tall; brand logo 40px; lightbox full-screen; reserve form 1-col with 48px inputs; scroll-progress-ring moved to 14px from corner; back-to-top 14px 44x44; safe-area-inset-bottom respected; nav-overlay 1.4rem font; pricing-card padding 26px 22px
  - **Tiny mobile (≤420px)**: h1 2.3rem, h2 1.85rem; gallery 1-col; nav overlay font 1.2rem; tighter padding everywhere
  - **Landscape phone (≤860px × ≤500px)**: hero adapts to short height
- **Mobile-specific infrastructure**:
  - `viewport-fit=cover` added to all 8 viewport meta tags (iPhone X+ notch support)
  - `env(safe-area-inset-*)` on `.site-header`, `.nav-overlay`, `.lightbox-close`, `.hero-stats` margin
  - `100svh` (small viewport height) on hero + lightbox with `100vh` fallback (cascade order: fallback first, modern second)
  - `-webkit-tap-highlight-color: transparent` on body (no tap flash)
  - `-webkit-text-size-adjust: 100%` on html (prevents iOS text inflation)
  - `overscroll-behavior-y: contain` on body (prevents chain-scrolling)
  - `touch-action: none` on `body.no-scroll` (prevents scroll-while-menu-open)
  - `overflow-x: hidden` on html + body (prevents horizontal scroll)
  - `font-size: 16px` on mobile form inputs (prevents iOS input zoom)
- **Mobile menu improvements** (script.js):
  - `openMobileMenu()` / `closeMobileMenu()` extracted to reusable functions
  - Close on outside-click of overlay (`e.target === navOverlay`)
  - Close on Escape key (returns focus to menu-toggle)
  - Auto-close on resize past 860px (in case of orientation change or window resize)
- **Accessibility on mobile**:
  - Site-header z-index bumped to 17 (above nav-overlay 16) so menu-toggle X stays clickable
  - `body.no-scroll .site-header:not(.page-header)` gets paper background + dark text when menu opens on transparent-header pages
  - `:focus-visible` outlines extended to `.gallery-filter`, `.audio-toggle`, `.menu-toggle`, `.back-to-top`, `.lightbox-close`, `.lightbox-nav`
  - FAQ question focus style with 2px clay outline
  - Gallery grid item: button reset (no default border/padding), `:focus-visible` 3px clay outline
- **Map overlay fix**: `pointer-events: none` on `.map-overlay` with `pointer-events: auto` on children — was blocking all iframe interaction on mobile
- **Map container**: `position: relative` added to `.map-container` so `.map-overlay` is scoped to the map (was relative to viewport before)
- **Lightbox improvements**:
  - Added `border-radius: 6px` desktop, `border-radius: 0` on mobile (full-bleed)
  - `max-height: 92svh` with `92vh` fallback
  - `width: 100vw; height: 100vh/svh` on small mobile for true full-screen
  - Added `.lightbox-counter` style (was unstyled, showing as plain text)
- **`prefers-reduced-motion` block consolidated and strengthened** — global `animation-duration: 0.01ms !important`, transitions disabled, scroll-behavior: auto, plus reveal/blur/stagger reset to `opacity: 1; transform: none; filter: none`
- **Hero floats hidden on mobile** (`.hero-floats { display: none }` in 860px) — empty decorative divs taking DOM space
- **Brand text** got `white-space: nowrap` to prevent awkward wrapping on small screens
- **Gallery categories fully corrected using Airbnb listing metadata** — extracted AI-generated `tags` (heroMedia edges) and human-curated `accessibilityLabel` (additional photos section) from the listing page's embedded JSON for all 60 images
- **Comment fixed** — `"58 images"` → `"60 images"` in `script.js:11`
- **All miscategorized images corrected** (7 in total):
  - `7fbc7015` living→bedroom (label "Bedroom 2 image 3")
  - `8ecff898` living→bedroom (hero AI tag `Bedroom`)
  - `240bf565` living→bedroom (label "Bedroom 1 image 1")
  - `60abf060` living→bedroom (label "Bedroom 1 image 4")
  - `a9aa74fe` living→bedroom (label "Bedroom 2 image 2")
  - `22647fec` pool→balcony (label "Balcony image 4")
  - `d33e925f` pool→balcony (label "Balcony image 6")
  - `7013f821` kitchen→living (label "Dining area image 5")
  - `717e52c4` exterior→pool (label "Pool image 4")
- **Final counts**: living 17, bedroom 12, exterior 12, balcony 8, pool 5, kitchen 3, bathroom 3 = 60
- **Background images cross-referenced** — all 11 background‑image UUIDs verified present in gallery and context‑relevant
- **Gallery filter logic verified** — 7 filter buttons match all 7 categories
- **Perspective fix verified** — `body` has no `perspective`; only `.hero[data-hero-tilt]` and `.band[data-depth]` have their own
- All earlier items remain done (scroll motion entrance-handoff, missing subhero bg-cover, CTA text contrast, mobile nav z-index, eyebrow contrast, FAQ answer clipping, scroll ring circumference, page titles consistency, `id="top"`, `aria-current="page"` on mobile nav, dead CSS removal, inline link visibility, transparent-header nav link color, stray CSS brace, h3 sizing, `loading="lazy"`, dead lazy-load polyfill removed, FAQ `aria-expanded`, back-to-top z-index raised, duplicate `applySkeleton()` removed, nav underline/transition, nav scroll transition, mobile tap targets, body perspective removed)
- **CSS file recovered after corruption (~580 lines restored)** — hero, subhero, tabs, feature cards, gallery, lightbox, FAQ, guidebooks, pricing, testimonials, booking form, split-list, cta-strip, back-to-top, scroll-progress-ring, nav-overlay, reveal animations all restored from scratch using HTML class analysis
- **Hero converted from data-hero-tilt (3D mouse tilt) to fixed-background parallax** — index.html hero now uses `bg-cover` + `data-motion-y` matching all subheros
- **amenities.html fixed** — orphaned `</article>`/`</div>` tags removed, duplicate old-format feature cards removed, CTA strip given `bg-cover` background image (was invisible white-text-on-white)
- **residence.html fixed** — mobile nav overlay given all 7 links (had only 1)
- **Broken grouped selector split** — `.brand, .nav-links, .hero-actions, .site-footer` separated into 4 standalone rules (was applying footer-level padding/layout to header elements)
- **Missing CSS added** — `.hero-actions` (`flex`, `gap: 12px`), `.nav-links` (`flex` + `align-items: center`), global `h1`/`h2` base styles, `.tab-panel h3/p`, `.feature-card img`, `.feature-content`, `.pricing-card .button.secondary`, `.guidebook-card` icon/locals/distance, `.pricing-card.featured::before` badge, `.testimonial-card .quote-icon`, gallery loading/lazy/hover, `<dialog>` lightbox
- **Horizontal padding unified** — hero-content changed from `clamp(22px,6vw,86px)` to `clamp(20px,5vw,72px)` matching all other sections; `.section-heading` given `margin: 0 auto 40px` for consistent vertical spacing
- **Features-grid double-padding removed** — stripped `max-width`/`margin`/`padding` from `.features-grid` (was conflicting with child `.feature-cards` constraints)
- **faq-list padding added** — `.faq-list` now has `padding: 0 clamp(20px,5vw,72px)` matching every other content container on the site (was missing, causing FAQ text to be flush with viewport edge on mobile)
- **FAQ content overhauled** — 12 categories, 35 questions with proper `<ul>` lists, `.faq-category` and `.faq-answer ul` styling
- **Site-wide quality improvements** (no design changes):
  - **JS bug fixes**: TDZ `ReferenceError` on `motionCache` (B1) by moving declaration above first `buildGrid` call; `NaN%` scroll meter guard (B5); `Number.isFinite` guard on count-up targets
  - **Audio reliability**: `currentTime` set after `loadedmetadata` (B2); autoplay retry on link click (B3); debounced `timeupdate` save (3s); `visibilitychange` auto-pause/resume; `Number.isFinite` guard on saved time
  - **Accessibility**:
    - `prefers-reduced-motion` short-circuits all reveal, tilt, depth-parallax, hero-float, and scroll-motion effects (no transform applied)
    - Skip-to-content link added to all 8 pages (visible on focus)
    - `aria-pressed` on gallery filter buttons (synced with `.active` class on click + on load)
    - `aria-controls` + `id` + `role="region"` on all 35 FAQ item pairs
    - `role="alert"` + `aria-live="polite"` on form status
    - Focus trap in lightbox `Tab`/`Shift+Tab` cycles within dialog
    - Audio toggle button in header (40px circle, updates icon and persists state)
  - **Form improvements** (reserve.html):
    - Name + Email required fields with `autocomplete` hints
    - Date inputs get `min` set to today; checkout min auto-updates when checkin changes
    - Validation: past dates, `checkout <= checkin`, malformed email, empty fields
    - `novalidate` on form so JS owns the validation messaging
    - `form-status` gets `success` / `error` visual variants in moss/clay tones
  - **Pricing card fix** (reserve.html): "3+ Nights" card (which violated the no-per-night-rate rule) replaced with "View on Airbnb" card linking to the official listing
  - **SEO**:
    - `og:type`, `og:title`, `og:description`, `og:image`, `og:locale` on all 8 pages
    - `twitter:card` (summary_large_image) on all 8 pages
    - `<link rel="canonical">` on all 8 pages (placeholder `havenharaya.com` domain)
    - JSON-LD `LodgingBusiness` schema on index.html (address, geo, amenities, check-in/out)
    - `theme-color` meta tag on all 8 pages
    - `sitemap.xml` + `robots.txt` created in root
  - **Defensive coding**:
    - `applySkeleton` `load`/`error` listeners use `{ once: true }` to avoid leaks
    - `try`-equivalent guards on `parseInt`/`parseFloat` for user-influenced data
  - **Iframe fallback**: `location.html` Google Maps iframe now has a `<p>` fallback link for browsers that block embeds

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- **Gallery category hierarchy**: `accessibilityLabel` (human-curated names from additional photos section, e.g. "Bedroom 2 image 3") → hero AI `tags` (first non-PhotoRanker tag) → host `caption` → fallback. The accessibility labels from the image carousel are the most reliable signal; they consistently describe what the photo actually shows.
- `Dining_room` maps to `living` (no separate dining filter); `Backyard` maps to `exterior`; `Patio` maps to `balcony`; `Office` maps to `bedroom`; `Half_bathroom`/`Full_bathroom` map to `bathroom`
- `8410e020` kept as `exterior` despite AI tag `Pool` — alt says "Basketball court", no court category exists
- `body { perspective: 1200px }` removed to fix `position: fixed` breakage in Chrome 131+; `.band[data-depth]` and `.hero[data-hero-tilt]` retain their own perspective since they're the only elements using `translateZ`
- **CSS reconstructed from scratch** after file corruption — used HTML class analysis to rebuild missing rules; ~580 lines restored; 1998 lines final size
- **`.faq-list` padded** — was the only content container missing horizontal padding; added `clamp(20px,5vw,72px)` to match all other sections; narrower `max-width:720px` kept for readability
- **Hero padding unified** — from `clamp(22px,6vw,86px)` to `clamp(20px,5vw,72px)` so all sections share the same horizontal padding
- **Wix-style effects added** (preserving existing design):
  - **CSS** (~360 lines added in `styles.css` after line 2333): `reveal-words` (word-by-word), `reveal-letters` (letter-by-letter), `reveal-clip-img` (image clip mask), `reveal-mask-up` (slide-up mask), `reveal-blur` (blur+slide), `reveal-from-left/right` (side reveal), `reveal-stagger-up` (children fade-up with 80ms stagger), `heading-line` (clay line draw), `underline-draw` (text underline draw), `reveal-number` (number mask), `parallax-bg` + `[data-parallax-speed]` (multi-layer), `magnetic` (magnetic buttons), `hover-zoom` + `.hover-caption` (image scale + caption overlay), `glow-border` (gradient border on hover), `card-lift` (lift on hover), `icon-scale` (icon scale+rotate on hover), `horizontal-track` (horizontal scroll)
  - **JS** (~150 lines added in `script.js` after line 644): `splitText()` wraps words/letters in `<span>`; `initTextSplits` runs once; `initGenericReveals`/`observeNewReveals` (single shared observer; observeNewReveals re-runs for dynamic gallery items); `initStaggerReveal`; `initParallax` (rAF-throttled scroll listener, `[data-parallax-speed]` controls); `initMagnetic` (mouse tracking, disabled on `pointer:coarse` and `max-width:620px`); `initColorTint`; `initWixEffects` master initializer runs on `DOMContentLoaded`
  - **HTML** (all 8 pages updated): every `<h1>`/`<h2>` got `data-reveal-words`; subhero p got `reveal-blur`; Reserve nav + all `.button` (primary/secondary/ghost) + nav-action got `magnetic`; intro/page-card-grid/listing-grid/testimonials-grid/detail-grid/pricing-cards/guidebook-cards/feature-cards got `reveal-stagger-up`; cards got `card-lift` + `glow-border`; page cards got `hover-zoom` + `.hover-caption`; gallery items rendered with `reveal-mask-up` + `hover-zoom` + `card-lift` in `script.js` `buildGrid()`; guidebook icons got `icon-scale`; intro `<h2>` got adjacent `<span class="heading-line">`
  - All new effects respect `prefers-reduced-motion` (short-circuits to `visible` or returns early)
  - Gallery items use `reveal-mask-up` which slides a charcoal mask up to reveal the image; combined with `hover-zoom` they also get the image scale + caption overlay effect
  - `observeNewReveals()` re-runs after `buildGrid()` rebuilds the gallery so dynamic items get observed
- **Mobile overflow fixes** (h1/h2 word-span clipping at 478px headless viewport):
  - h1 at 620px: 2.8rem → 1.9rem (with `overflow-wrap: break-word`)
  - h2 at 620px: 1.35rem → 1.1rem (with `overflow-wrap: break-word; line-height: 1.25`)
  - h1 at 420px: 2rem → 1.7rem
  - h2 at 420px: 1.5rem → 1.2rem
  - `.subhero h1` at 620px: 2.4rem → 1.8rem
  - `.section-heading h2` at 620px: 1.5rem → 1.1rem
  - Base h1/h2: added `overflow-wrap: break-word`
  - `.reveal-words .word`: added `max-width: 100%` to prevent word-span overflow clipping
  - `.faq-question`: added `overflow-wrap: break-word; min-width: 0` to prevent long question text from overflowing
- **Header mobile fix** (menu-toggle was invisible — `display: none` at all viewports):
  - **Root cause**: base `.menu-toggle { display: none }` (line 2361) came AFTER the 860px media query that set `display: flex`, so the base rule won by CSS cascade ordering
  - Fix: changed base `.menu-toggle` to `display: flex` and added `@media (min-width: 861px) { .menu-toggle { display: none } }` to hide it on desktop
  - Removed `margin-left: auto` from `.audio-toggle` at 860px (was pushing menu-toggle off-screen)
  - Added `flex-shrink: 0` to all `.site-header > *` children (mobile + 420px)
  - 420px breakpoint: hides `.brand-text` (just shows logo), smaller button sizes (38px audio, 42px menu-toggle, 38px nav-action min-height)
  - **Headless Chrome viewport quirk**: `--window-size=375,812` produces an actual viewport width of 478px (verified via JS: `window.innerWidth=478`). Real mobile devices at 375-414px will trigger the 420px breakpoint correctly. The 375px screenshots are scaled from 478px, making thin elements (like 2px hamburger lines) hard to see. At the actual 478px viewport, all elements are visible and properly spaced.
- **Dark mode + page loader**:
  - `[data-theme="dark"]` token block; `initTheme` IIFE in `script.js`; `localStorage.haven-theme`; sun/moon icon swap
  - FOUC-prevention inline `<script>` in all 8 `<head>` blocks (reads `localStorage` → `prefers-color-scheme` → `light`/`dark`); also accepts `?theme=light` or `?theme=dark` URL param for headless testability
  - Page loader `<div class="page-loader">` first child of `<body>`; 2s min / 4s max; `sessionStorage.haven-loader-shown`; `<noscript>` fallback
  - `body { transition: background-color, color 400ms ease }`
- **Dark mode hardcoded-color audit** (5 fixes, all `var(--charcoal)` → `var(--ink)` for text or themeable tokens):
  - `.detail-card h3` L731: was `var(--charcoal)` (resolves to #0a0807 in dark) → `var(--ink)` (#f1ece2 light). **Caused invisible "Sleeping Setup" / "Living & Dining" / "Kitchen & Bath" / "Private Balcony" headings on residence detail panels in dark mode.**
  - `.intro-copy h2` L623: was `var(--charcoal, #24231f)` → `var(--ink)`. Would have caused invisible H2 in dark mode.
  - `.detail-number` L748: was `rgba(0, 0, 0, 0.04)` → `var(--ink-faint)` (decorative 01/02/03/04 watermark now visible on dark surface)
  - `.form-status.error` L2985: was hardcoded `#8a3f24` → `var(--clay)`. Was unreadable on dark bg.
  - `.hero .eyebrow, .dark .eyebrow` L354: was hardcoded `#e8bf88` (light-mode brass) → `var(--brass)` (themeable)
- **Dark mode hardcoded-background audit** (4 fixes, all hardcoded cream/white → `var(--surface)`):
  - `.detail-panel` L855: `#f8f3ea` → `var(--surface)` (dark = #1f1a15)
  - `.tab-panel` L1013: `#f8f3ea` → `var(--surface)`
  - `.booking-form` L1267: `#f8f3ea` → `var(--surface)`
  - `.badge` L660: `white` → `var(--surface)`
  - Discovered: actual card class is `detail-panel` (HTML uses `<article class="detail-panel card-lift">`), not `detail-card`
- **Color rule established**: `var(--charcoal)` may ONLY be used for `background-color`, NEVER for `color:`. In dark mode, --charcoal = #0a0807 which is invisible on dark surfaces. Use `var(--ink)` for all text instead.
- **Dark mode hardcoded-background audit** (4 fixes, all hardcoded cream/white → `var(--surface)`):
  - `.detail-panel` L855: `#f8f3ea` → `var(--surface)` (dark = #1f1a15)
  - `.tab-panel` L1013: `#f8f3ea` → `var(--surface)`
  - `.booking-form` L1267: `#f8f3ea` → `var(--surface)`
  - `.badge` L660: `white` → `var(--surface)`
  - Discovered: actual card class is `detail-panel` (HTML uses `<article class="detail-panel card-lift">`), not `detail-card`
- **Color rule established**: `var(--charcoal)` may ONLY be used for `background-color`, NEVER for `color:`. In dark mode, --charcoal = #0a0807 which is invisible on dark surfaces. Use `var(--ink)` for all text instead.
- **White-on-white conflict in `.intro` section** (index.html L160):
  - **Root cause**: `.intro` had hardcoded `background: linear-gradient(145deg, #fffaf5 0%, #fef7ec 100%);` (light cream gradient) that didn't theme. In dark mode, `.intro-copy h2` (uses `var(--ink)` = light) and `.intro-copy p` (uses `var(--ink-soft)` = light) became invisible on the still-light cream background.
  - **Fix**: Added `[data-theme="dark"] .intro` rule with dark gradient `linear-gradient(145deg, #1f1a15 0%, #15110d 100%)`. Also added dark overrides for `.intro::before` (lighter dot pattern) and `.section-kicker` (warmer clay background).
  - **Verified** via dedicated test page in `C:\Users\HP\AppData\Local\Temp\opencode\chrome-test\intro-test-*.png` — both modes render with proper contrast.
  - **Pattern established**: When a section has a hardcoded decorative gradient for visual interest in light mode, add a dark-mode override to swap to a dark gradient that matches the theme surface token family.
- **Sticky mobile Book Now CTA** (all 8 pages):
  - `<aside class="sticky-cta">` with price "AED 698" + "2 nights min" + brass "Reserve →" button
  - **Visible**: only on mobile (≤860px), only after `scrollY > 400`, hidden when footer in view, hidden when lightbox/menu open
  - **Styling**: dark inner bar with brass button, cream paper gradient backdrop, `env(safe-area-inset-bottom)` for iPhone X+ notches
  - **JS**: rAF-throttled scroll listener; `?sticky=1` URL param for headless testing; "From" eyebrow + Georgia serif price; 44px+ touch target
  - **Body padding-bottom: 80px** on mobile to prevent content being hidden under the CTA (overridden when lightbox/menu open)
  - **Reduced motion**: `transition: none` on the CTA, instant show/hide
  - **Dark mode**: linear-gradient uses dark paper color, button uses brass token, inner uses `--ink` (light cream)
  - All 8 HTML files updated; `styles.css` +28 lines (3784→3935), `script.js` +50 lines
- **Dark mode `var(--ink)` background bug** (7 elements, all fixed — `var(--ink)` becomes light in dark mode, breaking dark-text-on-dark-bg → invisible):
  - `.button.primary` L431: changed `background: var(--ink)` → `var(--charcoal)` in `.booking-form .button.primary` and `[data-theme="dark"]` override
  - `.cta-strip:not(.dark) .button.primary` L1428-1431: → `var(--charcoal)`
  - `.site-footer` L1336: `background-color: var(--ink)` → `var(--charcoal)` (was light cream bg with white text in dark mode)
  - `.nav-action[aria-current="page"]` L300: → `var(--charcoal)` (active nav link)
  - `.skip-link` L3005 + border-color L3100: → `var(--charcoal)`
  - `.back-to-top:hover` L3098: → `var(--charcoal)` (hover state was invisible)
  - `.site-header.scrolled .nav-action:hover` L3227: → `var(--charcoal)`
  - `.sticky-cta-inner` L3825: → `var(--charcoal)` (the pill on mobile CTA)
- **Color rule strengthened**: `var(--ink)` may ONLY be used for `color:` properties in the *base* light theme, NEVER for `background-color` or `background:`. Use `var(--charcoal)` instead, which resolves to `#24231f` (light) or `#0a0807` (dark) — both consistently DARK across themes. For elements that should be ALWAYS dark (footer, buttons, skip-link, sticky CTA, active states), use `var(--charcoal)` as background.
- **Amenities tabs color bug** (`.tab` L1004-1016): tabs had no explicit `color:`, were rendering dark on dark in dark mode. Added `color: var(--ink)`, hover state, and `transition`. Active tab in dark mode (`.tab.active` + `[data-theme="dark"] .tab.active` L1018-1027) now uses `color: var(--charcoal)` (dark text on light sage pill) for high contrast — was white-on-pale-sage before.
- **Mojibake cleanup** (13 instances across 5 files): UTF-8 bytes were being interpreted as CP1252 then re-encoded as UTF-8 (showing as `â€"`, `â‰ˆ`, etc.). Fixed:
  - `reserve.html` L86: `₱11,800` (peso sign) — was `â,±11,800`
  - `reserve.html` L87: `~ AED 698 total` — was `â‰ˆ AED 698 total`
  - `residence.html` L124: em-dash `—` — was `â€"`
  - `faq.html` L97: en-dash `–` in `20–30 minutes` — was `â€"`
  - `faq.html` 2 more em-dashes, `guidebooks.html` 6 em-dashes, `location.html` 1 em-dash
- **`?nopreload=1` URL param** added to `initPageLoader` (script.js L989) — skips the page loader entirely for headless testing
- **Bash heredoc issue in PowerShell** discovered: PowerShell can't parse `b'...'` byte strings; use a `.py` script file and `python <file>` instead

## Next Steps
- (Optional) Audit remaining `rgba(255,255,255,...)` and `#fff` color uses to ensure each is in a `.dark`/dark-section context (low priority — all current uses verified OK).
- (Optional) Restructure CSS so base rules come before media queries (~700 line move) to fix cascade ordering.
- (Optional) Test on real device: system pref sync, localStorage persistence, FOUC absence, lightbox/FAQ/form on touch.
- (Optional) Add Plausible analytics, image optimization (srcset/WebP).

## Critical Context
- Airbnb listing URL: `https://www.airbnb.ae/rooms/1629801564228977399` — page embeds structured JSON with `tags` (AI classification) and `accessibilityLabel` (human-curated names like "Bedroom 2 image 3", "Living room image 5", "Pool image 2") for each image; data was extracted from raw scrape
- All 60 gallery UUIDs mapped to Airbnb metadata in the corrected `script.js` array
- Host profile (guidebooks source): `https://www.airbnb.ae/users/profile/1629801564953275334`
- AED ↔ PHP rate used: ~16.85 (midpoint for June 2026)
- All 8 pages share a common header/footer pattern and the same `script.js` + `styles.css`
- 10 guidebook IDs fetched: 6899578, 6899764, 6899816, 6899819, 6899820, 6899827, 6899834, 6899837, 6899842, 6899844
- Image base URL: `https://a0.muscache.com/im/pictures/hosting/Hosting-1629801564228977399/original/`
- Background images used (by UUID): `b3debb9b` (footer all pages), `b7a2b92a` (residence subhero), `ff03a03f` (gallery subhero), `e32e9834` (faq subhero), `35d721b5` (guidebooks subhero), `9ad3f274` (reserve subhero), `c15df783` (amenities subhero + index CTA), `51b7fe78` (residence CTA), `6a4cafc5` (location subhero + CTA), `d33e925f` (gallery CTA), `c4b02746` (guidebooks CTA)

## Relevant Files
- `script.js`: Gallery array fully corrected with categories from Airbnb metadata; comment on line 12; gallery filter logic (lines 198–223); all 7 image corrections listed above
- `styles.css`: `body` no `perspective` (line 24–33); `.hero[data-hero-tilt]` perspective (line 2277); `.band[data-depth]` perspective (line 2479); `.nav-links a` underline removed/transition added (lines 208–225); `.nav-overlay a` padding 8px 16px (line 1818); `--clay: #9e583c` (line 8); `.faq-item.open .faq-answer` max-height 1200px (line 1928)
- `gallery.html`: Filter buttons (lines 63–70): All, Living, Kitchen, Bedroom, Bathroom, Balcony, Pool, Exterior
- `C:\Users\HP\.local\share\opencode\tool-output\tool_e8e690253001xQL56J2g1ivawx`: Raw scrape — additional photos section (lines 2270–2550) with accessibility labels; heroMedia edges (lines 3823–4245) with AI tags
